<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <%= link_to "チームアウエル会計管理",  root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navmenu" aria-controls="navmenu" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navmenu">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <% if current_user.try(:admin?) %>
                        <%= link_to "
                        Admin",  rails_admin_path, class: "nav-link active" %>
                    <% end %>
                </li>
                <li class="nav-item">
                    <% if current_user.try(:admin?) %>
                    <%= link_to "Logout",  destroy_user_session_path, class: "nav-link active" ,method: :delete%>
                    <% else %>
                    <%= link_to "Login",  rails_admin_path, class: "nav-link active" %>
                    <% end %>
                </li>
            </ul>
        
        </div>
</nav>
</header>
